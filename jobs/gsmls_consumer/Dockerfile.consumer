FROM python:3.11-slim

# 1. System deps (only if needed)
RUN apt-get update && apt-get install -y \
    git \
    curl \
    gcc \
    && rm -rf /var/lib/apt/lists/*

# 2. Set working directory
WORKDIR /app

# 3. Install Python deps (cache-friendly)
COPY jobs/gsmls_consumer/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 4. Copy job code + plugins
COPY jobs/gsmls_consumer .

# 5. Entrypoint
ENTRYPOINT ["python", "/app/jobs/gsmls_consumer/gsmls_consumer.py"]